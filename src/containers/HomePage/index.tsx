import CountryCard from '../../components/CountryCard';
import React, { useEffect } from 'react';

const HomePage = () => {
    useEffect(() => {});
  return <CountryCard countryCode='AU' countryName='Australia' />;
};

export default HomePage;
